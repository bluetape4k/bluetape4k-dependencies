[versions]
demo-version = "1.0.0"

[libraries]
demo = { module = "org.example:demo", version.ref = "demo-version" }

[plugins]
example-plugin = { id = "org.example.plugin", version.ref = "demo-version" }
